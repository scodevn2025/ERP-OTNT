{
  "name": "erp-robot-vacuum",
  "version": "1.0.0",
  "description": "ERP System for Robot Vacuum Business",
  "scripts": {
    "postinstall": "cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm start",
    "backend": "cd backend && uvicorn server:app --reload --host 0.0.0.0 --port 8000",
    "backend:prod": "cd backend && uvicorn server:app --host 0.0.0.0 --port 8000"
  },
  "keywords": ["erp", "inventory", "sales", "accounting"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
